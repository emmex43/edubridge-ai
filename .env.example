# Variables consumed by docker-compose.yml for the optional local Postgres.
# Every one has a default, so this file is only needed to override them.
#
# The backend itself reads backend/.env, NOT this file -- see backend/.env.example.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=edubridge
